<h3>Friends of <%= person.name %></h3>

<ul>


<% person.friends.each do |friend| %>

    <li><%= link_to friend.name, person_url(friend) %> <%= link_to "Add friend »", create_friend_url(friend), method: :post unless friend.friend_of?(current_user.person) || friend == current_user.person %></li>

<% end %>

</ul>