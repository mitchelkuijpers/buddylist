%aside#side-content
  = render partial: "users/sidebar", locals: { user: current_user }

#main-content
  .island

    = render partial: "posts/new", locals: { receiver: current_user }
    = render partial: newsfeed.posts, as: :post
