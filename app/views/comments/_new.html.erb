<%= form_tag add_comment_url do %>
    <%= text_field_tag :comment_text %>
    <%= hidden_field_tag :commentable_id, commentable.id %>
    <%= hidden_field_tag :commentable_type, commentable._type %>
    <%= submit_tag "Add Comment" %>
<% end %>